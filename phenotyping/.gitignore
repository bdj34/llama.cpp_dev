# Runtime working dir: notes, inputs, LLM outputs, .done markers, logs, results.
# Nothing under runtime/ is source; it is regenerated by run_task.sh.
runtime/**
!runtime/.gitkeep

# Kerberos ticket caches or credential scratch, if ever written here.
*.ccache
krb5cc_*
